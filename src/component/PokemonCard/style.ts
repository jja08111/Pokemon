import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  pokemonCard: {
    padding: 16,
    borderRadius: 20,
    flexDirection: 'column',
    alignItems: 'center',
  },
  image: {
    width: 88,
    height: 88,
    borderRadius: 16,
  },
});
